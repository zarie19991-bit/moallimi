name: Deploy NAFES admin function
on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/nafes-admin/**'
      - '.github/workflows/deploy-supabase-admin.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Supabase deployment token
        id: token
        shell: bash
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          if [ -n "$SUPABASE_ACCESS_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "SUPABASE_ACCESS_TOKEN is not configured; function deployment skipped."
          fi
      - uses: supabase/setup-cli@v1
        if: steps.token.outputs.available == 'true'
        with:
          version: latest
      - name: Deploy nafes-admin
        if: steps.token.outputs.available == 'true'
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy nafes-admin --project-ref udznpifopbnrcgxtpzza --no-verify-jwt
