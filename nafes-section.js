(()=>{
const NAFES_SUBJECTS=['arabic','math','science'];
const META={arabic:{title:'القراءة — لغتي الخالدة',icon:'ق',soft:'#e4f1ee',color:'#0f514c'},math:{title:'الرياضيات',icon:'∑',soft:'#f6eddf',color:'#986a25'},science:{title:'العلوم',icon:'ع',soft:'#edf1e6',color:'#667b33'}};
let nafesSubject='arabic',nafesView='overview';
let frameworkRows=[],bankRows=[],indicatorAssessments=[],indicatorAttempts=[];
let loaded=false,loading=null;
const isNafesAssessment=a=>String(a?.title||'').includes('نافس')||String(a?.term||'')==='نافس';
const profileFor=s=>state.profiles.find(p=>p.subject_key===s&&p.grade_level==='الثالث المتوسط');
const rowsFor=s=>frameworkRows.filter(r=>r.subject_key===s).sort((a,b)=>(+a.sort_order||0)-(+b.sort_order||0));
const countIndicators=s=>rowsFor(s).reduce((n,r)=>n+(Array.isArray(r.indicators)?r.indicators.length:0),0);
const safeId=s=>String(s||'').replace(/[^a-zA-Z0-9_-]/g,'-');
function choose(n,k){try{n=BigInt(n);k=BigInt(k);if(k<0n||n<k)return 0n;if(k>n-k)k=n-k;let r=1n;for(let i=1n;i<=k;i++)r=r*(n-k+i)/i;return r}catch{return 0n}}
function formatBig(v){try{const b=BigInt(v);if(b<=9007199254740991n)return new Intl.NumberFormat('ar-SA').format(Number(b));return 'أكثر من 9,000,000,000,000,000'}catch{return '0'}}
function bankFor(outcome,no,subject=nafesSubject){return bankRows.find(b=>b.subject_key===subject&&b.outcome_code===outcome&&+b.indicator_no===+no)||{approved_questions:0,pending_questions:0,models_for_9_questions:0}}
function assessmentsFor(subject=nafesSubject){return indicatorAssessments.filter(a=>a.subject_key===subject)}
function attemptsFor(aid){return indicatorAttempts.filter(x=>x.assessment_id===aid)}
async function loadNafes(force=false){
 if(loaded&&!force)return;if(loading&&!force)return loading;
 const f='/rest/v1/nafes_framework_2026?select=subject_key,domain_name,subdomain_name,outcome_code,outcome_title,indicators,sort_order,source_name,source_context&grade_level=eq.'+encodeURIComponent('الثالث المتوسط')+'&is_active=eq.true&order=subject_key.asc,sort_order.asc';
 loading=Promise.all([req(f),req('/rest/v1/nafes_indicator_bank_status?select=*&grade_level=eq.'+encodeURIComponent('الثالث المتوسط')),req('/rest/v1/nafes_indicator_assessments?select=*&order=created_at.desc'),req('/rest/v1/nafes_indicator_attempts?select=*&order=started_at.desc')]).then(([fw,br,as,at])=>{if(!fw.ok)throw new Error(fw.msg||'تعذر تحميل إطار نافس');frameworkRows=fw.data||[];bankRows=br.ok?br.data||[]:[];indicatorAssessments=as.ok?as.data||[]:[];indicatorAttempts=at.ok?at.data||[]:[];loaded=true;loading=null}).catch(e=>{loading=null;throw e});return loading;
}
function domainGroups(subject){const out=[];for(const row of rowsFor(subject)){let g=out.find(x=>x.name===row.domain_name);if(!g){g={name:row.domain_name,rows:[]};out.push(g)}g.rows.push(row)}return out}
function subjectCards(){return NAFES_SUBJECTS.map(k=>{const m=META[k],ready=bankRows.filter(b=>b.subject_key===k&&+b.approved_questions>=25).length;return `<button class="nafes-subject ${nafesSubject===k?'active':''}" style="--nafes-soft:${m.soft};--nafes-color:${m.color}" onclick="setNafesSubject('${k}')"><span class="icon">${m.icon}</span><h4>${m.title}</h4><p>${rowsFor(k).length} ناتج · ${countIndicators(k)} مؤشر</p><span class="count">${ready} مؤشر بنكُه جاهز</span></button>`}).join('')}
function localNav(){const x=[['overview','نظرة عامة'],['framework','المهارات والمؤشرات'],['training','التدريب'],['tests','الاختبارات'],['results','النتائج والعلاج']];return `<nav class="nafes-local-nav">${x.map(([k,t])=>`<button class="${nafesView===k?'active':''}" onclick="setNafesView('${k}')">${t}</button>`).join('')}</nav>`}
function indicatorRow(row,text,no){const b=bankFor(row.outcome_code,no),approved=+b.approved_questions||0,pending=+b.pending_questions||0,models=choose(approved,9),ready=approved>=25;return `<div class="nafes-indicator nafes-indicator-work"><b>${no}</b><div class="nafes-indicator-main"><span>${esc(text)}</span><div class="nafes-bank-meta"><em class="${ready?'ready':'hold'}">${approved} سؤال معتمد</em><em>${pending} قيد المراجعة</em><em>${formatBig(models)} نموذج ممكن من 9 أسئلة</em></div></div><div class="nafes-indicator-actions">${ready?`<button class="btn" onclick="openIndicatorAssessment('${row.subject_key}','${row.outcome_code}',${no},${JSON.stringify(text).replace(/"/g,'&quot;')},'training')">تدريب</button><button class="btn primary" onclick="openIndicatorAssessment('${row.subject_key}','${row.outcome_code}',${no},${JSON.stringify(text).replace(/"/g,'&quot;')},'test')">اختبار</button>`:`<span class="nafes-bank-lock">يُفتح عند 25 سؤالًا معتمدًا</span>`}</div></div>`}
function outcomeCard(row,index){const inds=Array.isArray(row.indicators)?row.indicators:[],id='official-'+safeId(row.outcome_code);return `<section class="nafes-goal nafes-official-outcome"><div class="nafes-goal-head"><div class="nafes-goal-no">${index+1}</div><div><div class="nafes-outcome-meta"><span>${esc(row.outcome_code)}</span><span>${esc(row.subdomain_name||row.domain_name)}</span><span>${inds.length} مؤشر</span></div><strong>${esc(row.outcome_title)}</strong></div></div><button class="nafes-indicator-toggle" onclick="toggleOfficialNafesIndicators('${id}',this)">عرض المؤشرات والبنوك</button><div id="${id}" class="nafes-indicators nafes-indicators-collapsed"><div class="nafes-indicators-title">المؤشرات التفصيلية وحالة بنك كل مؤشر</div>${inds.map((x,j)=>indicatorRow(row,x,j+1)).join('')}</div></section>`}
function overviewView(){const total=countIndicators(nafesSubject),ready=bankRows.filter(b=>b.subject_key===nafesSubject&&+b.approved_questions>=25).length,approved=bankRows.filter(b=>b.subject_key===nafesSubject).reduce((n,b)=>n+(+b.approved_questions||0),0),tests=assessmentsFor(),open=tests.filter(a=>a.status==='open').length;return `<section class="nafes-section-panel"><div class="nafes-section-title"><div><span>01</span><h3>حالة نافس الفعلية</h3><p>لا توجد اختبارات وهمية. كل مؤشر له بنك مستقل، ولا يُفتح التدريب أو الاختبار حتى يصل إلى 25 سؤالًا معتمدًا على المؤشر نفسه.</p></div></div><div class="nafes-status-grid"><article class="ready"><b>المؤشرات الرسمية</b><strong>${total}</strong><small>للصف الثالث المتوسط</small></article><article class="review"><b>المؤشرات الجاهزة</b><strong>${ready}/${total}</strong><small>25 سؤالًا معتمدًا فأكثر</small></article><article class="review"><b>الأسئلة المعتمدة</b><strong>${approved}</strong><small>خاصة بهذه المادة</small></article><article class="review"><b>اختبارات مفتوحة</b><strong>${open}</strong><small>من المحرك الجديد فقط</small></article></div><div class="nafes-safe-note ${ready?'good':''}"><b>قاعدة النماذج</b><span>عند وجود 25 سؤالًا معتمدًا للمؤشر، فإن اختيار 9 أسئلة يعطي 2,042,975 تركيبة مختلفة قبل احتساب تغيير ترتيب الخيارات. ويمكن إنشاء عدد غير محدود من الاختبارات على المؤشر نفسه؛ كل طالب يأخذ نموذجًا مختلفًا من نفس الكود.</span></div><div class="nafes-workflow"><div><b>1</b><span>المؤشر الرسمي</span></div><i>←</i><div><b>2</b><span>25+ سؤالًا معتمدًا</span></div><i>←</i><div><b>3</b><span>ملايين النماذج</span></div><i>←</i><div><b>4</b><span>كود واحد للفصل</span></div><i>←</i><div><b>5</b><span>نتيجة المؤشر</span></div><i>←</i><div><b>6</b><span>علاج وقياس بعدي</span></div></div></section>`}
function frameworkView(){const groups=domainGroups(nafesSubject);let oi=0;return `<section class="nafes-section-panel"><div class="nafes-section-title"><div><span>02</span><h3>المهارات والمؤشرات الرسمية</h3><p>داخل كل مؤشر ترى الآن عدد الأسئلة المعتمدة، وعدد الأسئلة تحت المراجعة، وعدد النماذج الممكنة، ثم تنشئ تدريبًا أو اختبارًا من المؤشر نفسه.</p></div></div>${groups.map(g=>`<section class="nafes-domain-group"><div class="nafes-domain-head"><div><small>المجال</small><h3>${esc(g.name)}</h3></div><div><b>${g.rows.length}</b><span>نواتج</span><b>${g.rows.reduce((n,r)=>n+(r.indicators?.length||0),0)}</b><span>مؤشرات</span></div></div><div class="nafes-goals">${g.rows.map(r=>outcomeCard(r,oi++)).join('')}</div></section>`).join('')}</section>`}
function trainingView(){const rows=assessmentsFor().filter(a=>a.assessment_mode==='training'),open=rows.filter(a=>a.status==='open');return `<section class="nafes-section-panel"><div class="nafes-section-title"><div><span>03</span><h3>التدريب على المؤشرات</h3><p>التدريب لا يدخل في التحصيل النهائي. لكل تدريب مؤشر واحد واضح، ويمكن تكرار المحاولات وإظهار الحل فورًا حسب الإعداد.</p></div></div><div class="nafes-training-path"><article><b>البداية</b><span>تعرف المطلوب في المؤشر</span></article><article><b>أتدرب</b><span>أسئلة موجهة من البنك المعتمد</span></article><article><b>أتقدم</b><span>نموذج مستقل مختلف</span></article><article><b>أطبق</b><span>سياق أو بيانات جديدة</span></article><article><b>أتقن</b><span>ينتقل الطالب لقياس الإتقان</span></article></div>${open.length?`<div class="test-list">${open.map(testRow).join('')}</div>`:'<div class="nafes-safe-note"><b>لا يوجد تدريب مفتوح حاليًا.</b><span>افتح تبويب «المهارات والمؤشرات»، ثم أنشئ التدريب مباشرة من المؤشر الذي اكتمل بنكُه.</span></div>'}</section>`}
function testRow(a){const cls=state.classes.find(c=>c.id===a.class_id),ats=attemptsFor(a.id),done=ats.filter(x=>x.status==='submitted'||x.status==='timed_out').length;return `<div class="test-row"><div><h4>${esc(a.title)}</h4><p>${esc(cls?.name||'')} · ${a.question_count} أسئلة · ${a.duration_minutes} دقيقة · ${done} نتيجة</p><small>${esc(a.indicator_text)}</small></div><div class="actions"><span class="status ${a.status==='open'?'high':'neutral'}">${a.status==='open'?'مفتوح':a.status==='closed'?'مغلق':'مسودة'}</span>${a.status==='open'?`<span class="test-code">${esc(a.access_code)}</span><button class="btn" onclick="shareIndicatorAssessment('${a.id}')">كود / QR</button><button class="btn" onclick="setIndicatorAssessmentStatus('${a.id}','closed')">إغلاق</button>`:`<button class="btn primary" onclick="setIndicatorAssessmentStatus('${a.id}','open')">فتح</button>`}</div></div>`}
function testsView(){const rows=assessmentsFor().filter(a=>a.assessment_mode!=='training');return `<section class="nafes-section-panel"><div class="nafes-section-title"><div><span>04</span><h3>اختبارات نافس على المهارات</h3><p>يمكنك إنشاء أكثر من 50 اختبارًا على المؤشر نفسه، بل عدد غير محدود من سجلات الاختبار. كل سجل يستخدم بنك المؤشر ويولد للطلاب نماذج مختلفة على الخادم.</p></div></div><div class="nafes-test-policy"><b>الحماية المطبقة</b><span>25+ سؤالًا معتمدًا</span><span>نموذج مختلف لكل طالب</span><span>ترتيب خيارات مختلف</span><span>حفظ تلقائي</span><span>وقت وإغلاق</span><span>منع تغيير النموذج بإعادة الدخول</span></div>${rows.length?`<div class="test-list">${rows.map(testRow).join('')}</div>`:'<div class="nafes-safe-note"><b>لا توجد اختبارات من المحرك الجديد بعد.</b><span>أنشئ الاختبار من بطاقة المؤشر بعد اكتمال بنك الأسئلة المعتمد.</span></div>'}</section>`}
function resultsView(){const ass=assessmentsFor(),rows=indicatorAttempts.filter(x=>ass.some(a=>a.id===x.assessment_id)&&(x.status==='submitted'||x.status==='timed_out')),byStudent=new Map();for(const r of rows){if(!byStudent.has(r.student_id))byStudent.set(r.student_id,[]);byStudent.get(r.student_id).push(r)}const st=[...byStudent.entries()].map(([id,rs])=>{const vals=rs.map(x=>+x.percentage).filter(Number.isFinite),avg=vals.length?vals.reduce((a,b)=>a+b,0)/vals.length:null;return{id,name:state.students.find(s=>s.id===id)?.full_name||rs[0]?.student_name_snapshot||'طالب',avg,count:rs.length,low:rs.filter(x=>+x.percentage<60)}});return `<section class="nafes-section-panel"><div class="nafes-section-title"><div><span>05</span><h3>النتائج والعلاج</h3><p>كل محاولة من المحرك الجديد تُحفظ على المؤشر، ثم تُنسخ نتيجتها تلقائيًا إلى ملف التحصيل الدراسي للطالب.</p></div></div><div class="nafes-result-flow"><article><b>النتيجة</b><span>درجة الطالب في المؤشر</span></article><article><b>التشخيص</b><span>المؤشرات الأقل من حد الإتقان</span></article><article><b>التدريب</b><span>يعود للمؤشر نفسه فقط</span></article><article><b>القياس البعدي</b><span>نموذج جديد من نفس البنك</span></article></div><div class="table-wrap"><table class="table"><thead><tr><th>الطالب</th><th>المحاولات</th><th>المتوسط</th><th>محاولات تحتاج علاجًا</th></tr></thead><tbody>${st.map(s=>`<tr><td><button class="student-link" onclick="openStudent('${s.id}')">${esc(s.name)}</button></td><td>${s.count}</td><td>${pct(s.avg)}</td><td>${s.low.length}</td></tr>`).join('')||'<tr><td colspan="4">لا توجد نتائج من المحرك الجديد حتى الآن.</td></tr>'}</tbody></table></div></section>`}
function renderSubjects(){const area=$('subjectsArea');if(state.subjectDetail){renderSubjectDetail();return}const cards=Object.entries(SUBJECTS).map(([k,s])=>{const ps=state.profiles.filter(p=>p.subject_key===k),tests=state.assessments.filter(a=>ps.some(p=>p.id===a.subject_profile_id)&&!isNafesAssessment(a)),tasks=state.assignments.filter(a=>(a.subject_key||'arabic')===k);return `<button class="subject-card" style="--subject-soft:${s.soft};--subject-color:${s.color}" onclick="openSubject('${k}')"><span class="subject-icon">${s.icon}</span><h3>${s.name}</h3><p>دروس وتدريب واختبارات منهجية مرتبطة بالطالب.</p><div class="mini"><span>${tests.length} اختبار</span><span>${tasks.length} واجب/مشروع</span><span>بدون نافس</span></div></button>`}).join('');area.innerHTML=`<div class="subject-grid">${cards}</div><div class="soft-card" style="margin-top:14px"><b>التنظيم:</b> نافس منفصل بالكامل عن اختبارات المنهج والمواد.</div>`}
const baseRenderSection=renderSection;renderSection=function(n){if(n==='nafes')return renderNafes();return baseRenderSection(n)};
window.setNafesSubject=k=>{nafesSubject=NAFES_SUBJECTS.includes(k)?k:'arabic';nafesView='overview';renderNafes()};window.setNafesView=k=>{nafesView=['overview','framework','training','tests','results'].includes(k)?k:'overview';renderNafes()};window.toggleOfficialNafesIndicators=(id,btn)=>{const p=document.getElementById(id);if(!p)return;const open=p.classList.toggle('open');btn.textContent=open?'إخفاء المؤشرات والبنوك':'عرض المؤشرات والبنوك'};
window.openIndicatorAssessment=function(subject,outcome,no,text,mode='test'){
 const b=bankFor(outcome,no,subject),approved=+b.approved_questions||0;if(approved<25)return toast('هذا المؤشر لم يصل بعد إلى 25 سؤالًا معتمدًا',true);const classes=state.classes.filter(c=>c.grade==='الثالث المتوسط'&&c.is_active!==false);if(!classes.length)return toast('أضف فصلًا للثالث المتوسط أولًا',true);const defaultCode='NF-'+rand(7),defaultTitle=`نافس ${META[subject].title} — ${mode==='training'?'تدريب':'اختبار'} المؤشر ${outcome}.${no}`;modal(mode==='training'?'إنشاء تدريب على المؤشر':'إنشاء اختبار على المؤشر',`<form id="indicatorAssessmentForm" class="formgrid"><div class="soft-card full"><b>${esc(text)}</b><div style="font-size:10px;margin-top:5px">البنك: ${approved} سؤالًا معتمدًا · ${formatBig(choose(approved,9))} نموذجًا ممكنًا عند اختيار 9</div></div><div class="field"><label>الفصل</label><select name="class_id">${classes.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>عدد الأسئلة</label><input name="question_count" type="number" min="5" max="${Math.min(20,approved)}" value="9"></div><div class="field full"><label>اسم ${mode==='training'?'التدريب':'الاختبار'}</label><input name="title" value="${esc(defaultTitle)}" required></div><div class="field"><label>المدة بالدقائق</label><input name="duration" type="number" min="5" max="180" value="${mode==='training'?20:25}"></div><div class="field"><label>عدد المحاولات</label><input name="attempts" type="number" min="1" max="10" value="${mode==='training'?3:1}"></div><div class="field"><label>وقت الفتح</label><input name="opens_at" type="datetime-local"></div><div class="field"><label>وقت الإغلاق</label><input name="closes_at" type="datetime-local"></div><div class="field"><label>إظهار الإجابات</label><select name="show_answers"><option value="${mode==='training'?'immediate':'after_close'}">${mode==='training'?'فورًا بعد التدريب':'بعد إغلاق الاختبار'}</option><option value="immediate">فورًا</option><option value="after_close">بعد الإغلاق</option><option value="never">لا تظهر</option></select></div><div class="field"><label>الكود</label><input name="access_code" value="${defaultCode}" required></div><div class="field full"><label>الحالة</label><select name="status"><option value="draft">حفظ كمسودة</option><option value="open">فتح للطلاب الآن</option></select></div><button class="btn primary full">حفظ ${mode==='training'?'التدريب':'الاختبار'}</button></form>`,()=>{$('indicatorAssessmentForm').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target)),payload={teacher_id:state.user.id,class_id:f.class_id,subject_key:subject,outcome_code:outcome,indicator_no:+no,indicator_text:text,title:f.title,access_code:String(f.access_code).toUpperCase(),question_count:+f.question_count,duration_minutes:+f.duration,attempt_limit:+f.attempts,status:f.status,opens_at:f.opens_at?new Date(f.opens_at).toISOString():null,closes_at:f.closes_at?new Date(f.closes_at).toISOString():null,show_answers_mode:f.show_answers,min_verified_pool:25,assessment_mode:mode};const r=await req('/rest/v1/nafes_indicator_assessments','POST',payload,true);if(!r.ok)return toast(r.msg,true);closeModal();await loadNafes(true);nafesView=mode==='training'?'training':'tests';renderNafes();toast('تم إنشاء '+(mode==='training'?'التدريب':'الاختبار')+' وربطه بالمؤشر')}})};
window.setIndicatorAssessmentStatus=async function(id,status){const r=await req('/rest/v1/nafes_indicator_assessments?id=eq.'+id,'PATCH',{status,updated_at:new Date().toISOString()},true);if(!r.ok)return toast(r.msg,true);await loadNafes(true);renderNafes();toast(status==='open'?'تم فتح الاختبار':'تم إغلاق الاختبار')};
window.shareIndicatorAssessment=function(id){const a=indicatorAssessments.find(x=>x.id===id);if(!a)return;const link=new URL('nafes.html',location.href);link.searchParams.set('code',a.access_code);qrModal(a.title,a.access_code,link.href)};
function renderNafes(){const area=$('nafesArea');if(!area)return;if(!loaded){area.innerHTML='<div class="card"><div class="empty"><b>جارٍ تحميل نافس...</b></div></div>';loadNafes().then(()=>{if(state.section==='nafes')renderNafes()}).catch(e=>area.innerHTML=`<div class="card"><div class="empty"><b>تعذر تحميل نافس.</b>${esc(e.message||'')}</div></div>`);return}const m=META[nafesSubject],body=nafesView==='framework'?frameworkView():nafesView==='training'?trainingView():nafesView==='tests'?testsView():nafesView==='results'?resultsView():overviewView();area.innerHTML=`<section class="nafes-hero"><span class="kicker">نافس · الثالث المتوسط</span><h3>مؤشر رسمي → بنك معتمد → ملايين النماذج → نتيجة وعلاج</h3><p>لا يظهر تدريب أو اختبار من البنوك القديمة. المحرك الجديد يعمل فقط على الأسئلة المراجعة والمربوطة بالمؤشر الرسمي.</p></section><div class="nafes-subjects">${subjectCards()}</div>${localNav()}<div class="nafes-current-head"><div><span>المادة الحالية</span><h3>${m.title}</h3></div><small>${rowsFor(nafesSubject).length} ناتج · ${countIndicators(nafesSubject)} مؤشر</small></div>${body}`}
window.renderNafes=renderNafes;
})();